[project]
name = "dbt-ippon-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.8,<1.9",
    "dbt-snowflake>=1.8,<1.9",
    "fuzzywuzzy>=0.18.0",
]
