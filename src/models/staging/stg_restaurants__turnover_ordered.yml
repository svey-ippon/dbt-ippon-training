version: 2

models:
  - name: stg_ranking_restaurants 
    description: Stage to compute the restaurant's rank
    columns:
      - name: restaurant_identifier
        description: Primary key for restaurants
